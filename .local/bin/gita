#!/bin/sh

cmd='git'
mes='patch'

while getopts lcm: f; do
	case $f in
		l) cmd='cfgl';;
		c) cmd='cfgc';;
		m) mes="$OPTARG" ;;
		\?) exit 1;;
	esac
done

"$cmd" add -u
"$cmd" commit -m "$mes"
"$cmd" push
