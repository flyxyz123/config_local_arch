#!/bin/sh

cmd='git'

while getopts lc f; do
	case $f in
		l) cmd='cfgl';;
		c) cmd='cfgc';;
		\?) exit 1;;
	esac
done
shift $((OPTIND-1))

mes="${1:-patch}"

"$cmd" add -u
"$cmd" commit -m "$mes"
"$cmd" push
